{
  // 编译器选项配置
  "compilerOptions": {
    // 指定编译后的JavaScript版本为ES2016
    "target": "es2016",
    // 指定生成的模块格式为CommonJS
    "module": "commonjs",
    // 支持ES模块和CommonJS模块之间的互操作性
    "esModuleInterop": true,
    // 强制文件名大小写一致
    "forceConsistentCasingInFileNames": true,
    // 启用所有严格类型检查选项
    "strict": true,
    // 不允许隐式的any类型
    "noImplicitAny": true,
    // 启用严格的null检查
    "strictNullChecks": true,
    // 启用严格的函数类型检查
    "strictFunctionTypes": true,
    // 启用严格的bind、call和apply方法检查
    "strictBindCallApply": true,
    // 启用严格的属性初始化检查
    "strictPropertyInitialization": true,
    // 启用严格的内置迭代器返回类型检查
    "strictBuiltinIteratorReturn": true,
    // 不允许隐式的this类型
    "noImplicitThis": true,
    // 在catch块中使用unknown类型代替any类型
    "useUnknownInCatchVariables": true,
    // 始终以严格模式生成代码
    "alwaysStrict": true,
    // 不允许未使用的局部变量
    "noUnusedLocals": true,
    // 不允许未使用的参数
    "noUnusedParameters": true,
    // 跳过对库文件的类型检查
    "skipLibCheck": true
  },
  // 排除不需要编译的文件或目录
  "exclude": [
    "node_modules",
    "dist",
    "**/__tests__"
  ]
}